require('should');
var blog = require('../bin/blog.js');

describe('A blog application:\n', function() {
  beforeEach(function(done){ done(); });
  afterEach(function(done){ done(); });
  before(function(done){ done(); });
  after(function(done){ done(); });


});
